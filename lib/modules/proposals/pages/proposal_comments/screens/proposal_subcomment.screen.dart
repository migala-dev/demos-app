import 'package:flutter/material.dart';

class ProposalSubCommentScreen extends StatelessWidget {
  const ProposalSubCommentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
